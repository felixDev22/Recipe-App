<nav class="navbar navbar-expand-xl navbar-light bg-light">
  <div class="container">
    <div class="navbar-brand">
       Recipe App
    </div>
    <% if user_signed_in? %>
    <div class="navbar-nav">
      <ul class="navbar-nav me-auto mb-2 mb-xl-0 d-flex flex-row"> 
        <li class="nav-item m-3">
          <%= link_to "Foods", foods_path %>
        </li>
        <li class="nav-item m-3">
          <%= link_to "Recipies", recipes_path %>
        </li>
        <li class="nav-item m-3">
          <%= link_to "Public Recipies", '/public_recipes', class: "link-success recipes" %>
        </li>
      </ul>
    </div>
    <% end %>

    <div class="d-flex align-items-center justify-content-between">
      <% if @current_user %>
      <div class="navbar-brand fs-6">Welcome
        <span><%= current_user.name %></span>
      </div>
      <div class="navbar-brand fs-6">
        <%= button_to "logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
      </div>
      <% else %>
      <div class="navbar-brand fs-6">
        <%= link_to "Login", '/users/sign_in', class: "btn btn-outline-secondary list-none" %>
      </div>

      <div class="navbar-brand fs-6">
        <%= link_to "Sign up", '/users/sign_up', class: "btn btn-outline-secondary" %>
      </div>
      <% end %>
    </div>
  </div>
</nav>
